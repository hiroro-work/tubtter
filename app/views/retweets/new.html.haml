.p-1
  .tweet-card
    %h4.card-header #{@retweet.tweet.user.name} ・ #{time_ago_in_words(@retweet.tweet.created_at, include_seconds: true)}前
    .card-body= simple_format(@retweet.tweet.content)

.padding-form
  = simple_form_for [@retweet.tweet, @retweet] do |f|
    = render 'retweets/form', f: f
