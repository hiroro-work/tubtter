.reply-card
  %h4.card-header #{@reply.user.name} ・ #{time_ago_in_words(@reply.tweet.created_at, include_seconds: true)}前
  .card-body= simple_format(@reply.tweet.content)
.padding-form
  = simple_form_for [@reply.user, @reply] do |f|
    = render 'replies/form', f: f
