- replies.each do |reply|
  .p-1
    .record
      .card.text-white.bg-secondary
        .card-header #{reply.user.name} ・ #{time_ago_in_words(reply.created_at, include_seconds: true)}前
        .card-body= simple_format(reply.content)
      %a{ href: user_reply_path(reply.user, reply), method: :get }
= paginate replies
